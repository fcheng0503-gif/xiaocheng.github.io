<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>丞丞丞 · 快捷收银台</title>
    <style>
        :root {
            --bg-color: #0b0f19;
            --card-bg: #131b2e;
            --text-color: #f3f4f6;
            --text-muted: #9ca3af;
            --primary-color: #6366f1;
            --wechat-color: #10b981;
            --alipay-color: #0284c7;
            --border-color: rgba(255, 255, 255, 0.06);
        }

        * { box-sizing: border-box; margin: 0; padding: 0; }

        body {
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
            background-color: var(--bg-color);
            color: var(--text-color);
            line-height: 1.6;
            padding: 40px 20px;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            background-image: radial-gradient(circle at 50% 30%, rgba(99, 102, 241, 0.08) 0%, transparent 60%);
        }

        .pay-container {
            background-color: var(--card-bg);
            border: 1px solid var(--border-color);
            border-radius: 24px;
            width: 100%;
            max-width: 580px;
            padding: 35px;
            box-shadow: 0 20px 50px rgba(0,0,0,0.5);
            backdrop-filter: blur(10px);
        }

        .header { text-align: center; margin-bottom: 30px; }
        .header h1 { font-size: 1.8rem; color: #fff; font-weight: 800; margin-bottom: 6px; letter-spacing: 1px; }
        .header p { color: var(--text-muted); font-size: 0.9rem; }

        .step-title { font-size: 0.95rem; color: #fff; font-weight: 700; margin-bottom: 12px; display: flex; align-items: center; gap: 8px; }

        /* 卡片选择 */
        .card-options { display: grid; grid-template-columns: repeat(3, 1fr); gap: 12px; margin-bottom: 25px; }
        .card-btn {
            background: rgba(0,0,0,0.2);
            border: 1px solid var(--border-color);
            border-radius: 12px;
            padding: 15px 10px;
            color: var(--text-color);
            cursor: pointer;
            transition: all 0.3s ease;
            text-align: center;
        }
        .card-btn .name { font-size: 0.95rem; font-weight: bold; display: block; margin-bottom: 4px; }
        .card-btn .price { font-size: 0.85rem; color: var(--text-muted); }
        
        .card-btn.active {
            border-color: var(--primary-color);
            background: rgba(99, 102, 241, 0.1);
            box-shadow: 0 0 15px rgba(99, 102, 241, 0.2);
        }
        .card-btn.active .price { color: #a5b4fc; font-weight: bold; }

        /* 输入邮箱 */
        .input-group { margin-bottom: 25px; }
        .input-group input {
            width: 100%;
            background: rgba(0,0,0,0.2);
            border: 1px solid var(--border-color);
            padding: 12px 16px;
            border-radius: 12px;
            color: #fff;
            font-size: 0.95rem;
            outline: none;
            transition: border-color 0.3s;
        }
        .input-group input:focus { border-color: var(--primary-color); }

        /* 金额大字显示 */
        .price-display { font-size: 2.5rem; font-weight: 800; text-align: center; margin-bottom: 20px; color: #fff; }
        .price-display span { font-size: 1.5rem; font-weight: normal; margin-right: 2px; }

        /* 双收款码合体网格 */
        .qr-combined-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 20px;
            margin-bottom: 25px;
        }
        
        .qr-channel-box {
            background: rgba(0, 0, 0, 0.15);
            border: 1px solid var(--border-color);
            border-radius: 16px;
            padding: 15px;
            text-align: center;
        }

        .qr-channel-box.wechat-zone { border-color: rgba(16, 185, 129, 0.2); }
        .qr-channel-box.alipay-zone { border-color: rgba(2, 132, 199, 0.2); }

        .channel-title {
            font-size: 0.85rem;
            font-weight: bold;
            margin-bottom: 12px;
            padding: 4px 10px;
            border-radius: 20px;
            display: inline-block;
        }
        .wechat-zone .channel-title { background: rgba(16, 185, 129, 0.15); color: #34d399; }
        .alipay-zone .channel-title { background: rgba(2, 132, 199, 0.15); color: #38bdf8; }

        .qrcode-wrapper {
            background: #fff;
            width: 100%;
            aspect-ratio: 1 / 1;
            max-width: 180px;
            margin: 0 auto;
            padding: 10px;
            border-radius: 12px;
            display: flex;
            align-items: center;
            justify-content: center;
            box-shadow: 0 8px 20px rgba(0,0,0,0.3);
        }
        .qrcode-wrapper img { width: 100%; height: 100%; object-fit: contain; }

        .notice {
            background: rgba(0,0,0,0.15);
            padding: 12px;
            border-radius: 10px;
            font-size: 0.8rem;
            color: var(--text-muted);
            text-align: center;
            border: 1px dashed rgba(255,255,255,0.05);
        }

        @media (max-width: 500px) {
            .qr-combined-grid { grid-template-columns: 1fr; gap: 15px; }
            .pay-container { padding: 25px 20px; }
        }
    </style>
</head>
<body>

<div class="pay-container">
    <div class="header">
        <h1>丞丞丞 · 收银台</h1>
        <p>自助购买 · 付款后自动下发卡密</p>
    </div>

    <div class="step-title"><span>01.</span> 选择订阅类型</div>
    <div class="card-options">
        <div class="card-btn active" onclick="selectCard(this, '天卡', '15.00')">
            <span class="name">⚡ 天卡</span>
            <span class="price">¥15.00</span>
        </div>
        <div class="card-btn" onclick="selectCard(this, '周卡', '100.00')">
            <span class="name">🚀 周卡</span>
            <span class="price">¥100.00</span>
        </div>
        <div class="card-btn" onclick="selectCard(this, '月卡', '300.00')">
            <span class="name">👑 月卡</span>
            <span class="price">¥300.00</span>
        </div>
    </div>

    <div class="step-title"><span>02.</span> 填入接收邮箱</div>
    <div class="input-group">
        <input type="email" id="user-email" placeholder="输入接收卡密的邮箱地址...">
    </div>

    <div class="step-title"><span>03.</span> 扫码支付金额</div>
    
    <div class="price-display"><span>¥</span><span id="current-price">15.00</span></div>
    
    <div class="qr-combined-grid">
        <div class="qr-channel-box wechat-zone">
            <div class="channel-title">🟢 微信扫码</div>
            <div class="qrcode-wrapper">
                <img src="wechat.png" alt="微信收款码">
            </div>
        </div>

        <div class="qr-channel-box alipay-zone">
            <div class="channel-title">🔵 支付宝扫码</div>
            <div class="qrcode-wrapper">
                <img src="alipay.png" alt="支付宝收款码">
            </div>
        </div>
    </div>

    <div class="notice">
        💡 提示：当前为静态测试模式，任选通道支付后系统将通过后台验证发货。
    </div>
</div>

<script>
    let currentPrice = '15.00';

    // 卡片类型切换逻辑
    function selectCard(element, name, price) {
        document.querySelectorAll('.card-btn').forEach(btn => btn.classList.remove('active'));
        element.classList.add('active');
        document.getElementById('current-price').innerText = price;
        currentPrice = price;
    }
</script>

</body>
</html>